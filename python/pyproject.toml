[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "abstraction-model"
version = "0.1.0"
description = "Whether two strings from two hosts name one model."
license = "Apache-2.0"
requires-python = ">=3.9"
dependencies = []

[project.urls]
Source = "https://github.com/openabstractions/abstraction-model"

[tool.setuptools]
py-modules = ["abstraction_model"]
